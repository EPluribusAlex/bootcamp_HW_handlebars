<li>
	{{name}}

	<button class="change-eaten" data-id="{{id}}" data-newdevoured="{{devoured}}">
		{{#if devoured}}Burger eaten!{{else}}Fresh burger!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>